[project]
name = "claude-historia"
version = "0.1.0"
description = "Lokalny serwer MCP: przeszukiwalna pamięć rozmów Claude Code (FTS5 + wektory)"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.0",
    "fastembed>=0.4",
    "numpy>=1.26",
]

[tool.uv]
package = false
