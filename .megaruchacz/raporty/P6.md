# P6 - jeden blok zasad kierownika dla Claude Code, odchudzenie, stan w .megaruchacz (2026-09-25)

Wersja 0.21.0. Specyfikacja: `.megaruchacz/raporty/P5.md` (przeniesiony z dawnego katalogu raportow).

## Co zrobione

1. **Nowy szablon** `szablony-global/claude/zasady-kierownika.md` (10 192 zn., CRLF jak inne szablony) -
   wersja dla Claude Code, zrodlo prawdy. Z dawnego projektowego CLAUDE.md, bez "Cisza jest zakazana",
   rejestr/mapa w `.megaruchacz/`, "rejestr START/KONIEC pisze hook", mapa w 3 punktach, limit raportu
   i "WYMAGA DECYZJI/SendMessage" przeniesione do rol, skrocone: Policz, Zastepca, Wiele identycznych,
   formulka twierdzenia. **Zostaly w calosci merytorycznie:** twierdzenie o mechanice (2026-09-16)
   + uzupelnienie o PowerShellu (2026-09-25), sprzatanie galezi, "zanim skasujesz kopie - sprawdz
   scalenie" (utrata pracy 2026-09-16: bez `&&`, alarm przy "Already up to date", commit w kryterium).
2. **Instalator** (`narzedzia/instaluj-globalnie.ps1`): wariant per plik.
   - `~/.claude/CLAUDE.md` <- wariant Claude Code, gdy Claude Code na maszynie PRACUJE (jest
     `~/.claude/history.jsonl` albo `~/.claude.json` - to samo rozroznienie co straznik i koszt-pamieci;
     na domowej `claude.exe` lezy w PATH, ale Claude Code nie jest uzywany). W przeciwnym razie
     wariant opencode/Codex jak dotad. Wymuszenie: `-WariantZasad claude|opencode`.
   - `~/.codex/AGENTS.md` <- `szablony-opencode/zasady-kierownika.md` - BEZ ZMIAN (jak dotad).
   - Konce linii bloku jak w pliku docelowym (dotad blok CRLF wpadal do pliku LF - mieszane konce).
   - Odmowa zapisu przy dwoch blokach kierownika (dubel) + samosprawdzenie "dokladnie jeden"
     i "w wariancie X".
3. **Projektowy `CLAUDE.md`**: 1 732 zn. - jedna linia "zasady sa globalne, zrodlo tu, wgrywa instalator"
   + "Cisza jest zakazana" w calosci.
4. **Stan w jednym miejscu `.megaruchacz/`**: `git mv` mapy (z niezacommitowanymi zmianami kierownika,
   ~42 KB, nic nie ucieto; 7 wpisow w mapie o starych sciezkach poprawionych + nowa sekcja "Zasady
   kierownika - zrodla i warianty"), dawny rejestr kierownika DOPISANY (append, bez przepisywania - hook
   pisze rownolegle) na koniec `.megaruchacz/worklog.md` pod naglowkiem "Scalone 2026-09-25 (P6)";
   sprawdzone skryptem: nowy plik zawiera caly dawny rejestr i caly poprzedni. Raporty tez przeniesione
   do `.megaruchacz/raporty/` (P6.md lezy tutaj). Odwolania poprawione: `mr-log.js` (projektowy szablon
   pisze teraz do `.megaruchacz/worklog.md`, zaklada katalog), `wdroz.ps1` (puste pliki stanu w
   `.megaruchacz\` zamiast kopiowania MAPY I REJESTRU TEGO REPO do cudzego projektu - to byl blad),
   `koszt-pamieci.ps1 -Warstwy`, `nowe-zadanie.ps1`, `rozszerzenie/extension.js`,
   `zasobnik/stan-nadzorcy.ps1` (komentarz), README, `.gitignore`, komentarze w lore (db.py, recall.py,
   dwa README).
5. **Role**: jedno zrodlo `szablony-global/claude/agents/`. Projektowe `.claude/agents/*.md` USUNIETE
   (przeslanialy globalne; `effort: high` z nich nic nie zmienia - `settings.json` ma `effortLevel: high`).
   `wdroz.ps1` i straznik (`Nanies-Poprawki`) biora role i zasady z `szablony-global`. Implementer:
   limit 5 linii + `.megaruchacz/raporty/<ID>.md`, "SendMessage do main, a bez niego raport WYMAGA
   DECYZJI". Scout: `tools` + Edit, Write (kazal pisac mape, nie dajac narzedzia).
6. **Przypomnienie**: 619 -> 345 zn. (repo `.claude/orchestrator-reminder.json`, kopie globalna
   odswiezyl straznik przy instalacji).
7. **koszt-pamieci.ps1**: pozycja "CLAUDE.md projektu" w starcie sesji przy `-Projekt` (pod Claude Code,
   nie gdy projekt = katalog domowy); bez `-Projekt` rachunek zostaje "maszynowy" (tak liczy linie
   straznik) - swiadomie. W `-Warstwy` bez dubla (warstwa `claude-projekt` juz byla). Rada "gdzie skracac"
   i "kto pisze" blok kierownika wskazuje szablon po naglowku wariantu. Mapa/rejestr w `-Warstwy` z
   `.megaruchacz\`.
8. **Straznik**: `Pilnuj-Zasad` bez zmian (nie dotyka bloku kierownika). Komentarz "duplikat" poprawiony
   (od 0.21.0 prawdziwy duplikat; wczesniej nie byl - opisane). Nie pilnuje aktualnosci bloku kierownika
   wzgledem szablonu - po zmianie szablonu trzeba odpalic instalator (jak dotad).

## Wdrozenie na tej maszynie

- Wlasciwe: **instalator globalny** (`-BezPytania`). `wdroz.ps1` przy stojacym znaczniku globalnym konczy
  sie od razu ("dziala GLOBALNIE") - nie jest droga aktualizacji.
- Kopia: `C:\Users\Primo\.claude\CLAUDE.md.przed-P6.bak` (md5 df13417e...). Instalator zrobil wlasna
  `CLAUDE.md.bak-20260925-141711`.
- Porownanie (skrypt node, bajty): czesc PRZED blokiem kierownika ("Co wiem" + blok `MegaRuchacz:start`)
  **identyczna bajt w bajt** (9 010 B), czesc po bloku identyczna, blokow kierownika 1 -> 1, naglowek
  "(Claude Code)", 0 znakow CR (plik byl i jest LF). Plik: 20 569 -> 18 893 zn.
- Globalne kopie: `~/.claude/agents/*` (nowe role; UWAGA: byly LF, teraz CRLF - tak kopiuje instalator
  z repo), `~/.claude/megaruchacz-mr-log.js` (odswiezony, CRLF jak byl), `~/.claude/mr/orchestrator-reminder.json`.
  `settings.json` - instalator i straznik NIC nie zmienily (md5 przed/po ten sam).

## Proby (uruchomione)

- Symulacja domowej (katalog tymczasowy z `.codex` i `.claude` bez `history.jsonl`), STARY instalator
  (git archive HEAD) vs NOWY: `~/.codex/AGENTS.md` i `~/.claude/CLAUDE.md` tresciowo identyczne (diff bez
  CR pusty), oba w wariancie "(opencode / Codex CLI)"; role Codeksa i opencode identyczne; hooki rozne
  tylko sciezka zrodla. Roznica jedyna: stary wstawial blok CRLF do pliku LF (mieszane konce), nowy LF.
- Dom z Claude Code + Codex: CLAUDE.md -> "(Claude Code)", AGENTS.md -> "(opencode / Codex CLI)";
  drugi przebieg "blok juz aktualny" (idempotencja).
- Negatywne: dwa bloki w pliku -> BLAD, plik nietkniety (md5 ten sam), "Instalacja NIEPELNA".
  `-WariantZasad opencode` na domu z CC -> podmiana na opencode. Brak bloku Lore -> straznik wpisuje go
  raz, blok kierownika bez zmian (md5), drugi przebieg bez zmian.
- Straznik na prawdziwym domu, 3 przebiegi: `~/.claude/CLAUDE.md` md5 bez zmian, 1 blok, `settings.json`
  bez zmian; odswiezyl tylko `megaruchacz-mr-log.js` (konce linii po poprawce szablonu).
- `wdroz.ps1` kroki 1-3 (kopia skryptu uciety po kroku 3, projekt i dom tymczasowe - pelny wdroz rusza
  Harmonogram Windows, wiec calosci nie puszczalem): role z szablony-global, puste `.megaruchacz\worklog.md`
  i `mapa.md`, zasady "(Claude Code)". Projektowy `mr-log.js` z payloadem -> zaklada `.megaruchacz\` i
  dopisuje START. Straznik na tym wdrozeniu (0.20.1 -> 0.21.0): odtworzyl skasowana role, podmienil zasady.
- `koszt-pamieci.ps1`: domyslny, `-Zwiezle`, `-Dane`, `-Rozbicie`, `-TylkoSufity`, `-Warstwy` (z i bez
  `-Projekt`) - dzialaja; `-Warstwy` JSON parsuje sie, bez Uwag. Parser PowerShella: 0 bledow w 6 plikach;
  `node --check` mr-log.js x2, extension.js; `py_compile` db.py, recall.py.
- Efekt uboczny prob: straznik na domu tymczasowym odpalil cykl wiedzy na tym domu - 0 kawalkow, nic nie
  zapisal poza plikami w katalogu tymczasowym.

## Tokeny (znaki/3, koszt-pamieci.ps1 -Rozbicie; "przed" = nowy kod na starych plikach)

| | przed | po |
|---|---:|---:|
| start sesji - TEN projekt | ~11 940 | ~6 869 |
| wiadomosc - ten projekt | ~207 | ~115 |
| start sesji - INNE projekty | ~6 850 | ~6 291 |
| wiadomosc - inne projekty | ~207 | ~115 |

Pozycje: blok kierownika 3 981 -> 3 422; CLAUDE.md projektu 5 090 -> 578; Lore/Wiedza 807 i "Co wiem"
2 062 bez zmian. Stary kod rachunku pokazywal dla tego projektu 6 850 (nie liczyl CLAUDE.md projektu).

**Prog "sesja powyzej 5 000 tokenow" - NIE miesci sie**: 6 869 (ten projekt) i 6 291 (inne); alarm
swieci dalej (kod 1). Najdrozsza pozycja: blok kierownika 3 422 (54% w innych projektach). Progu nie
ruszalem. Drugi alarm "wzrost +61% od 25.09 08:15" zniknie po jutrzejszym raporcie 08:15.
Linia straznika w cache (`+207 / +2881, nic nie jest ucinane`) jest stara - przeliczy sie w tle.

## Kodowanie (przed -> po)

Bez zmian BOM we wszystkich plikach (BOM tylko stan-nadzorcy.ps1, byl i jest). Skrypty .ps1 dalej czyste
ASCII, CRLF. Projektowy CLAUDE.md LF (byl LF). `~/.claude/CLAUDE.md` LF. Mapa CRLF, ASCII. Rejestr:
`.megaruchacz/worklog.md` mieszany jak byl (dopisany tekst z dawnego rejestru ma LF, BOM usuniety).
`sed -i` w Git Bash zdejmowal CR - przywrocone CRLF w ZMIANY.md, README.md, .gitignore, mr-log.js,
lore/*; ZMIANY.md mial 23 linie samego LF, teraz wszystkie CRLF (git i tak trzyma LF - autocrlf).

## Zostalo / do decyzji

1. **opencode na TEJ maszynie** czyta `~/.claude/CLAUDE.md` (sprawdzone w binarce opencode 1.18.32:
   globalnie pierwszy istniejacy z `~/.config/opencode/AGENTS.md`, `~/.claude/CLAUDE.md`), wiec dostaje
   teraz wariant Claude Code. Osobny wariant = wlasny `~/.config/opencode/AGENTS.md`, ale wtedy opencode
   traci "Co wiem" i blok Lore (trzeci cel dla `wpisz-zasady.ps1`). Instalator mowi o tym UWAGA. Domowej
   (Codex, `~/.codex/AGENTS.md`) to nie dotyczy.
2. **Twierdzenie o mechanice**: ja jako worker tego zlecenia NIE mialem narzedzia `SendMessage` (tylko
   Read/Write/Edit/Grep/Glob/Bash) - "prawo do pytania przez SendMessage" nie zawsze dziala. Dlatego rola
   implementera mowi "jesli masz SendMessage..., jesli nie - raport WYMAGA DECYZJI".
3. **Kierownik**: rejestr jest teraz w `.megaruchacz/worklog.md` (wpis [P6] tam, na koncu). Kierownik ma
   w kontekscie stary projektowy CLAUDE.md az do nowej sesji.
4. Stare mapy w innych repo na biurowej: `.claude/mapa.md` w Aliebaba-Primo, WMS_Official,
   amazon-helper, ecommerce-helper - globalny scout ich nie widzi (od 0.19.0). Nie ruszalem (cudze repo).
5. Rozszerzenie VS Code: zmiana w `extension.js` zadziala dopiero po przebudowaniu i reinstalacji VSIX.
6. Historyczne raporty (P5.md l.38, wdrozenie-codex.md l.35) i wpisy w rejestrze wspominaja stare
   sciezki - to historia, jak ZMIANY.md; nie przepisywalem.
7. `~/.claude/mr/megaruchacz-sesja.json` (stara wersja CC, nikt nie czyta) i `megaruchacz-zasady*.md`
   w `~/.claude/mr/` leza dalej - poza zakresem.
8. Blok kierownika po zmianie szablonu trzeba wgrac instalatorem - nikt tego nie pilnuje automatycznie
   (P5 to zauwazyl; bez zmian).
